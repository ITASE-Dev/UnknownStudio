//! Component library: `theme` tokens, `responsive` layout helpers, `core` primitives,
//! `components` composites. Every widget sizes itself from `ui.available_width()`;
//! `*_sized` variants take an explicit width.
#![allow(dead_code)]

pub mod components;
pub mod core;
pub mod imaging;
pub mod responsive;
pub mod theme;

